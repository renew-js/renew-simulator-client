{
  "name": "renew-simulator-client",
  "version": "1.0.0",
  "description": "Client library for the renew simulator gateway",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^5.12.0",
    "eslint-config-google": "^0.11.0",
    "eslint-config-renew": "^1.0.0"
  },
  "dependencies": {
    "socket.io-client": "^2.2.0"
  },
  "eslintConfig": {
    "root": true,
    "env": {
      "node": true,
      "browser": true
    },
    "extends": [
      "eslint:recommended",
      "google",
      "renew"
    ],
    "rules": {},
    "parserOptions": {
      "parser": "babel-eslint",
      "sourceType": "module"
    }
  }
}
